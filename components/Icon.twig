{% set context = fn('Timber::get_context') %}

{%
  set vars = {
    name: name,
    class: class,
    theme: context.theme
  }
%}

{% if context.WP_DEV %}
  {% include 'Icon.ref.twig' with vars only %}
{% else %}
  {% include 'build/components/Icon.ref.twig' with vars only %}
{% endif %}

